Running compare.py processes the input dataset for the three filtering configurations (no filter, inter-filter, and intra-filter) and generates a separate CSV file for each configuration. 
It needs the user to run python compare.py <file Path>
